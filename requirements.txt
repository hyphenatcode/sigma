# Sigma backend — pinned to the majors verified against the reference test suite.
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7
pydantic-settings>=2.3

# Statistics engine
pandas>=2.2
numpy>=1.26
scipy>=1.13
statsmodels>=0.14
pingouin>=0.5.4
openpyxl>=3.1

# Auth (§7: Supabase). [crypto] pulls in the RSA/ECDSA backends needed to
# verify Supabase's asymmetric ES256 tokens; cryptography is already required
# below for encryption at rest.
PyJWT[crypto]>=2.8

# Persistence
SQLAlchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9

# Interpretation (LLM layer — phrasing only, never computation)
anthropic>=0.30

# Export
python-docx>=1.1
Jinja2>=3.1
weasyprint>=62

# Privacy (encryption at rest, KVKK)
cryptography>=42

# Dev
pytest>=8.2
httpx>=0.27
